Get loading bar colors, if changed... (in css?/Random_Worker.js?)

Textures that still need editing (texture files and code in app.js):
* water
* lava

After all textures are done, create the last file (BACKGROUND.JPG)